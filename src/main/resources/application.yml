server:
  port: 9090

spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bluedot-studio/cloud-config-server-properties